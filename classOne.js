class ClassOne {
  constructor(model, year) {
    this.carModel = model;
    this.invYear = year;
  }

  one(data_one) {
    console.log(`Car Model: ${this.carModel} \nCar Invention Year: ${this.invYear} \nnew data is: ${data_one}`);
  }
}
const carOne = new ClassOne("BMW", 2025);
carOne.one("test");

Employee(Mamun, 2.5, $1000, "January");

// Mamun's salary for the month January is 2500
